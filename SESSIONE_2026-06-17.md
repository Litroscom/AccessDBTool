# Sessione di Sviluppo - AccessDBTool v7.4
**Data:** 2026-06-17
**Versione:** 7.2 → 7.4
**Skill attiva:** Superpowers (systematic-debugging)

---

## Modifiche effettuate

### Bug critici fixati
| # | Gravità | Problema | File |
|---|---------|----------|------|
| 1 | 🔴 Critico | Tab Controlli rotto (`frm_dyn` invisibile) | `views/builder_view.py` |
| 2 | 🟠 Alto | `_run_current()` non sincronizza Filtri/Esclusioni | `views/builder_view.py` |
| 3 | 🟠 Alto | Cambio tipo condizione non invalida sezioni | `views/builder_view.py` |
| 4 | 🟠 Alto | Menu "Salva" non sincronizza sezioni | `access_db_tool.pyw` |
| 5 | 🟡 Medio | Sezioni accordion chiuse all'apertura da dashboard | `views/builder_view.py` |
| 6 | 🟠 Alto | `except:` nudo in `_daily_coverage` | `engines.py` |
| 7 | 🟡 Medio | `_qi()` duplicata in 2 file | `engines.py` |
| 8 | 🟡 Medio | Quoting SQL non sicuro in `_get_sample` | `data_profiler.py` |
| 9 | 🟡 Medio | Estrazione fragile `mese`/`sett` | `engines.py` |
| 10 | 🟡 Medio | `load_condition()` non invalida sezioni | `views/builder_view.py` |

### Miglioramenti
- Sezioni Filtri/Esclusioni/Periodicità si **auto-espandono** quando si apre una condizione dal dashboard
- Versione bump: 7.3 → 7.4
- Nuovo spec file: `AccessDBToolPortable_v7_4.spec`

### Test
- 33/33 test passano (`pytest tests/test_logic_verification.py`)
- Import OK
- Compatibilità condizioni vecchie verificata con 56 condizioni sintetiche

---

## Struttura del progetto
```
AccessDBTool_v7.4/
├── access_db_tool.pyw    # Entry point GUI (tkinter + ttkbootstrap)
├── constants.py          # APP_VERSION, CONDITION_TYPES, OPERATORS, _BASE_DIR
├── engines.py            # ConditionExecutor, SimilarityEngine (14 tipi di check)
├── db_manager.py         # DatabaseManager (pyodbc, Access ODBC)
├── storage.py            # ConditionStore, MonitorLog, GroupStore, DatabaseRegistry
├── monitor_engine.py     # MonitorEngine (thread periodico)
├── data_profiler.py      # DataProfiler (insight automatici)
├── ui_components.py      # Tutti i builder UI (152 KB, 14 tipi)
├── theme_config.py       # Tema "superhero" ttkbootstrap
├── ui_helpers.py         # Scorciatoie Alt+*
├── logger_config.py      # Setup logging
├── build_portable.py     # Script PyInstaller per portable .exe
├── app/
│   ├── state.py          # AppState (thread-safe)
│   └── controllers/
│       ├── app_controller.py
│       ├── batch_controller.py
│       ├── db_controller.py
│       ├── library_controller.py
│       └── result_controller.py
└── views/
    ├── builder_view.py     # Tab Controlli (accordion con sezioni)
    ├── dashboard_view.py   # Tab Dashboard (batch)
    ├── database_view.py    # Tab Database
    ├── monitor_view.py     # Tab Monitor
    ├── results_view.py     # Pannello risultati (con menu contestuale)
    └── condition_manager_view.py  # Dialog gestione condizioni
```

## Flussi chiave
- **Esecuzione controllo:** BuilderView → ResultController.run_current() → ConditionExecutor.run(cond)
- **Batch dashboard:** DashboardView → BatchController.run_all_batch() → thread multi-db
- **Monitor:** MonitorView → AppController.start_monitor() → MonitorEngine thread periodico
- **Esclusioni:** ResultsView tasto destro → ResultController.add_exceptions_from_rows() → builder
- **Apri nel builder:** Dashboard → ResultsView → App._open_cond_in_builder() → BuilderView.load_condition()

## Comandi utili
```bash
# Eseguire l'app
python access_db_tool.pyw

# Test
python -m pytest tests/test_logic_verification.py -v

# Build portable
python build_portable.py

# Import da chiavetta USB
# Menu → Condizioni → Importa condizioni da file...
```

## Sessione 18/06 - Fix post-rilascio v7.4

### Bug fixati
| # | Problema | File |
|---|----------|------|
| 11 | `state.active_builder` non resettato al cambio tab | `app/controllers/app_controller.py` |
| 12 | `source_table` non riconosciuto (import condizioni legacy) | `views/builder_view.py`, `ui_components.py` |
| 13 | Esclusioni accordion non sincronizzate dopo `load_condition` | `views/builder_view.py` |
| 14 | Builder non scrollava quando il contenuto eccedeva | `views/builder_view.py` |

### Build
- Portable v7.4 rigenerata in `dist/AccessDBToolPortable_v7_4/`

### In sospeso
- Verifica con l'utente: la portable sul PC remoto (`\\Desktop-1nd1ueq\gelli\1_INBOX\AccessDBToolPortable_v7_4`) ha file v7.4 ma il log riporta ancora v7.2. Da verificare domani:
  1. Cancellare `app.log`
  2. Avviare l'app
  3. Controllare versione in barra del titolo
  4. Caricare una condizione similarity (es. "Nomi troppo simili Casa-quartiere")
  5. Inviare il nuovo `app.log`

## Sessione 19/06 - Fix allineamento versione e caricamento esclusioni

### Bug fixati
| # | Problema | File |
|---|----------|------|
| 15 | Log/versione riportava ancora v7.2 (stringhe hardcoded) | `access_db_tool.pyw` |
| 16 | Caricamento condizioni vecchie: esclusioni perdeva i dati se la sezione accordion era già espansa | `views/builder_view.py` |

### Dettaglio bug #16
Quando la sezione **Esclusioni** era già stata espansa dall'utente (o auto-espansa da una condizione precedente), `_invalidate_accordion_sections()` la ricostruiva con `active_builder=None`, creando un widget placeholder (`Label`). Successivamente `_sync_accordion_from_builder()` non riusciva a popolare le esclusioni perché il widget non era un `MultiConditionBuilder`.

**Correzione:**
- `_invalidate_accordion_sections()` ora distrugge solo i widget e marca `built=False`, senza ricostruire subito le sezioni espande.
- `_sync_accordion_from_builder()`, quando trova la sezione espansa senza widget, la ricostruisce con il builder attivo e poi vi riversa i dati correnti.

### Build
- `APP_BUILD_DATE` aggiornato a `2026-06-19` in `constants.py`
- Portable v7.4 rigenerata in `dist/AccessDBToolPortable_v7_4/`
- Verificato log della portable: ora riporta correttamente **v7.4**

### Bug #17: eccezioni singole ID non escludevano i record
| # | Problema | File |
|---|----------|------|
| 17 | Similarity: eccezioni singole tipo `"39716"` o `"ID:39716"` non escludevano il record | `engines.py` |
| 18 | Import condizioni: se esisteva già una condizione con lo stesso nome, veniva aggiunto un duplicato e il caricamento prendeva quella vecchia (senza eccezioni) | `app/controllers/library_controller.py` |
| 19 | "Aggiorna Salvata" non persisteva le esclusioni modificate nella sezione accordion: il widget Esclusioni usava `get_conditions()` (righe filtri) invece delle righe esclusioni; inoltre il pulsante "+ Aggiungi esclusione" aggiungeva righe in una sottosezione ignorata | `views/builder_view.py`, `ui_components.py` |
| 20 | "Aggiorna Salvata" non salvava le colonne da visualizzare modificate: `_on_builder_table_change` chiamava `col_selector.set_columns()` che distruggeva e ricreava tutte le checkbox, cancellando la selezione dell'utente | `views/builder_view.py` |
| 21 | Dopo "Esegui Controllo" dal builder, senza risultati non c'era feedback: ora mostra un messaggio "Nessun problema trovato" e, quando ci sono risultati, apre automaticamente il report nella Dashboard | `result_controller.py`, `access_db_tool.pyw` |

**Causa #18:** `import_conditions` faceva sempre `store.add(...)`, creando duplicati. Il caricamento da libreria prende la prima condizione con quel nome, quindi le eccezioni del file importato non venivano mai usate.

**Correzione #18:** l'import ora aggiorna (sovrascrive) la condizione esistente con lo stesso nome invece di crearne un duplicato. Conta separatamente nuove vs aggiornate.

**Causa #19:** il widget accordion "Esclusioni" era un `MultiConditionBuilder` che caricava le esclusioni nelle righe "conditions" (filtri) ma l'utente le aggiungeva col pulsante "+ Aggiungi esclusione" nelle righe "exclude" (ignorate al salvataggio). Risultato: le esclusioni modificate/non venivano mai salvate.

**Correzione #19:**
- `MultiConditionBuilder` supporta ora `show_conditions=False` (modalità solo-esclusioni): nasconde la sezione filtri e mostra solo le righe esclusioni.
- Il widget Esclusioni usa questa modalità e carica/legge le esclusioni via `exclude_conditions`/`get_exclude_conditions()`.
- `_sync_builder_from_sections` legge entrambe le sezioni (conditions + exclude) per robustezza.

**Causa #20:** ogni volta che si cliccava "Aggiorna" (o anche solo entrando nel builder), il callback `_on_builder_table_change` chiamava `col_selector.set_columns()`, che ricrea tutte le checkbox azzerandole. Così la selezione dell'utente veniva persa prima del salvataggio.

**Correzione #20:** `_on_builder_table_change` ora salva le selezioni prima di `set_columns` e le ripristina subito dopo.

**Causa:** `SimilarityEngine.find_similar()` trattava tutte le eccezioni singole come valori della colonna confrontata, mentre nel file di import molte erano ID numerici di record.

**Correzione:** aggiunto supporto per:
- eccezioni singole numeriche → escludono il record con quell'`key_column`
- eccezioni singole nel formato `ID:xxx` → stesso comportamento
- coppie `ID:xxx | ID:yyy` erano già supportate

### Test
- 35/35 test logici passano (`pytest tests/test_logic_verification.py`)
- Test manuale/scriptato di caricamento esclusioni: PASS

## Da fare / Idee future
- [x] `state.active_builder` non resettato al cambio tab (bug #11) - fixato 18/06
- [x] `source_table` non riconosciuto - fixato 18/06
- [x] Esclusioni accordion non sincronizzate - fixato 18/06
- [x] Scroll builder - fixato 18/06
- [x] Allineamento versione log/portable - fixato 19/06
- [x] Caricamento esclusioni condizioni vecchie - fixato 19/06
- [x] Eccezioni singole ID su similarity - fixato 19/06
- [ ] Normalizzazione `periodic_review_*` in `ConditionStore._normalize_condition` (allineare a `prepare_condition_for_storage`)
- [ ] Il doppio click nella dashboard riesegue il controllo (potrebbe aprire il builder)
- [ ] Aggiungere test UI automatici
- [ ] Unificare eccezioni/esclusioni in un'unica logica (UX)

---

## STATO FINE SESSIONE 19/06

**Versione:** v7.4 attiva

**Portable pronta:** `dist/AccessDBToolPortable_v7_4/`  
**Remoto da sostituire:** `\\Desktop-1nd1ueq\gelli\1_INBOX\AccessDBToolPortable_v7_4`

**Modifiche di oggi (non committate):**
| File | Modifica |
|---|---|
| `access_db_tool.pyw` | v7.2→v7.4 dinamico nel log, callback auto-switch dashboard |
| `constants.py` | APP_BUILD_DATE a 19/06 |
| `engines.py` | `find_similar`: supporto eccezioni singole ID numeriche e `ID:xxx` |
| `ui_components.py` | `MultiConditionBuilder` supporta `show_conditions=False` |
| `views/builder_view.py` | Fix caricamento esclusioni (bug #16), fix salvataggio esclusioni (#19), fix colonne report (#20), scroll repair |
| `app/controllers/library_controller.py` | Import con sovrascrittura duplicati (#18), fix display_columns in update |
| `app/controllers/result_controller.py` | Feedback "nessun problema" + auto-switch dashboard (#21) |
| `app/controllers/app_controller.py` | `state.active_builder` reset (18/06) |
| `app/controllers/db_controller.py` | (18/06) |
| `tests/test_logic_verification.py` | +2 test eccezioni ID singole (35 totali) |

**Bugs chiusi oggi:** #15 #16 #17 #18 #19 #20 #21
