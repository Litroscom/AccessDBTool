# Piano Remediation v7.3

Data: 2026-06-11
Branch: `fix/v7.3-hardening` (da `feature/v7-ui`)

## Principi
- Ogni fase = commit isolato + test prima di procedere.
- Bug dati (Fase 1, 2) → test prima del fix (TDD).
- Esecuzione parallela: agenti partizionati per file disgiunti (zero conflitti git).
- Refactor strutturale (split ui_components) per ultimo.

## Partizione file (per esecuzione parallela)
- **Agent1** → `db_manager.py`, `engines.py`
- **Agent2** → `app/state.py`, `app/controllers/*.py`
- **Agent3** → `monitor_engine.py`, `access_db_tool.pyw`
- File condivisi: NESSUNO (contratto cross-agent definito sotto).

### Contratto cross-agent
`db_manager.py` espone metodo transazionale (Agent1 lo implementa, Agent2 lo usa):

```python
def bulk_update(self, table, set_col, pk_col, pairs):
    """pairs = list[(typed_value, typed_pk)]. UPDATE singola transazione, un solo commit.
    Ritorna n righe modificate. Rollback su qualsiasi errore."""
```

---

## FASE 0 — Repo cleanup (rischio nullo)
- Commit `views/monitor_view.py` (untracked ma importato → tab Monitor crasha senza).
- `APP_VERSION = "7.3"` + `APP_BUILD_DATE` in `constants.py`.

## FASE 1 — bulk_replace perdita dati (CRITICO, TDD) — Agent2 + Agent1
- `result_controller.apply_bulk_replace`: se PK non identificata con certezza → errore, NO fallback `cols[0]`.
- Update via `db.bulk_update` (transazione singola, no commit per-riga).
- Ritorna conteggio + PK toccate; UI conferma "modificherai N righe" prima.

## FASE 2 — Concorrenza (CRITICO) — Agent2
- Worker batch NON scrive `dash_state_by_key` cross-thread.
- Pool raccoglie risultati per-thread → merge nel main thread (`root.after`) oppure lock in `AppState`.
- UI legge snapshot copia, mai dict mutante.

## FASE 3 — DatabaseManager — Agent1
- `connected` property → `is_open` (bool puro) + `ping()` (I/O esplicito con reset).
- Helper `_qi(name)` quoting identificatori: `[` + `name.replace("]","]]")` + `]`. Usato ovunque.

## FASE 4 — Semantica LIKE — Agent1
- `LIKE` = substring uniforme. Path Python `re.search` non `fullmatch`.
- `NOT LIKE` = negazione search.
- Parità SQL vs Python su filtri misti REGEXP.

## FASE 5 — Error handling — Agent2 + Agent3
- `except: pass` → log esplicito con contesto.
- Date-parse → helper `_parse_date(val)` con formati in lista + log.

## FASE 6 — Monitor + queue — Agent3 (monitor) + Agent2 (clear_mon_log)
- Busy-wait → `self._stop_event.wait(timeout=interval_sec)`.
- `clear_mon_log`: non toccare `queue.queue.clear()` interno. Drain via `get_nowait` o `mon_log.clear()`.

## FASE 7b — Rimuovi GroupStoreAdapter — Agent3
- `access_db_tool.pyw`: usare `state.group_store` diretto.

## FASE 8 — Minori — Agent1
- `find_similar`: cap righe input + warning se supera soglia (es. 50k).
- `cast_value`: fallback log su metadata mancante.

## FASE 7a — Split ui_components.py — DOPO (no parallelo)
- `ui/builders/` un file per gruppo. `ui_components.py` re-export retrocompat.
- Eseguito dopo merge fasi logiche, per non nascondere bug nei move.

---

## Test / verifica
- Estendere `tests/test_logic_verification.py`:
  - bulk_replace senza PK → errore.
  - LIKE SQL vs Python → parità.
  - `_build_where` con `]` nel nome colonna → quoting.
- Smoke manuale: apri DB, run 14 tipi, batch, monitor 1 ciclo.

## Ordine
`0 → (1,2,3,4,5,6,8 paralleli) → test → 7a`

## Rischio regressione
| Fase | Rischio | Mitigazione |
|------|---------|-------------|
| 0,7b,8 | basso | smoke |
| 1,2,3,4 | alto | TDD + smoke |
| 5,6 | medio | log review |
| 7a | medio | verifica import/avvio |
